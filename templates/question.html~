{% extends "base.html" %}

{% block content %}
<br><br>
{% include "que.html" %}
<hr>
{% if answers %}
	{% for a in answers %}
			<div class = "question">
				<div class = "left" >	
					<img  class = "avatar" src = "/uploads/{{a.author.avatar}}" >
					Reiting: {{ a.reiting_a }}<br>
					<bottom class ="btn controls"><img src = "/img/yes.png" class = "like" ></i></bottom>
					<bottom class ="btn controls b_right" ><img src = "/img/no.png" class = "like" ></i></bottom>
				</div>
				<div class = "right" >
					<h3> <a href = "/">{{a.author}}</a>	</h3>
					<p>{{a.answer_text}}</p>
					
					{% if a.flag %}
						<input type="checkbox" name="option1"  checked> 
					{%else%}
						<input type="checkbox" name="option1" > 
					{%endif%}		It's correct answer <br>
					<font class ="graywords">{{ a.pub_date }}</font>
				</div>
			</div>
		{% endfor %}
{% else %}
	<p>There are no answers yet</p><br>
{% endif %}
{% if user.is_authenticated %}
	<form  method = "post" action = '/que/{{question.id}}/answer'>
		{% csrf_token %}
		<textarea name="answer" cols="80" rows="4">Input your answer here</textarea>
  	<p><input type="submit" class = "btn-primary " value="Add answer"></p>
	</form>
{% endif %}
{% endblock %}
